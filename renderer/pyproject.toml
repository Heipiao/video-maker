[project]
name = "vowframe-renderer"
version = "0.1.0"
description = "Renderer worker for VowFrame ECI jobs"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["app*"]
